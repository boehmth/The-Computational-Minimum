# Runtime-Abhängigkeiten für "Milestones in Agentic Systems"
#
# Aufruf:
#   pip install -r requirements.txt

# Core — wird von jedem Kapitel gebraucht
python-dotenv>=1.0    # .env laden
requests>=2.31        # HTTP zu SAP GenAI Hub

# Google Gemini als Fallback-Backend (optional)
# Setze LLM_BACKEND=gemini in der .env, wenn du das statt SAP nutzen willst.
google-genai>=0.3

# Nur für Kapitel 5, Bonus-Sektion `a2a/` — der Live-Server
# Weglassen, wenn du diese Sektion nicht ausprobierst.
fastapi>=0.115
uvicorn>=0.30