#include <stdio.h>

/* Kanonische Aufgabe: (3 + 4) - 1 = 6.
   Zeigt Variablen, arithmetische Ausdruecke, printf. */

int main(void) {
    int x = 3;
    int y = 4;
    int z = (x + y) - 1;
    printf("%d\n", z);
    return 0;
}